export interface WheelModel {
    "id" : number,
    "totalParticipants": number,
    "title": string
    "createdAt": string
    "updatedAt": string
}
export interface WheelArrayModel{
    all_wheels: WheelModel[]
}